{
	"name": "crisis-resources-helplines-worker",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"stag": "wrangler dev --env staging",
		"prod": "wrangler dev --env production",
		"test": "vitest",
		"deploy": "wrangler deploy --env development --minify",
		"staging": "wrangler deploy --env staging --minify",
		"production": "wrangler deploy --env production --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings"
	},
	"dependencies": {
		"dotenv": "^17.2.0",
		"hono": "^4.8.5",
		"papaparse": "^5.5.3"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.56",
		"vitest": "^3.2.4",
		"wrangler": "^4.25.1"
	}
}